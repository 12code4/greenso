{
  "name": "greenso",
  "private": true,
  "version": "0.0.1",
  "description": "PLASTIC PLATOON (working title) — toy-soldier third-person shooter. M0 greybox.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "build:single": "tsc --noEmit && vite build -c vite.single.config.ts && cp dist-single/index.html plastic-platoon.html"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "playwright-core": "^1.62.1",
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vite-plugin-singlefile": "^2.3.3"
  }
}
